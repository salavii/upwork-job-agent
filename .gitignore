# Python
__pycache__/
*.pyc

# Private personal data - not meant to be shared/committed.
# See config.example.json for the expected shape of config.json.
config.json

# Private job data and generated output - not meant to be shared/committed.
# See jobs.example.json for the expected shape of jobs.json.
jobs.json
results.json
report.html
daily_report.html
job_list.json
jobs_backup.json
results_backup.json
daily_run_log.txt
ollama_serve_log.txt

# Old plain-text job log format (superseded by jobs.json), in case it
# still exists locally from an earlier version of this project.
jobs.txt

# Local/env files
.env
*.local
.claude/

# Local test/scratch logs from ad-hoc runs during development.
daily_run_manual_test*.txt
